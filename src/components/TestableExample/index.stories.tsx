import { Meta, StoryObj } from '@storybook/react'

import TestableExample from '.'

export default {
  title: 'TestableExample',
  component: TestableExample,
  parameters: {
    layout: 'fullscreen',
  },
} as Meta

export const Default: StoryObj = {}
